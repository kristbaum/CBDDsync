{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run exists_in_both as module",
      "type": "debugpy",
      "request": "launch",
      "module": "src.exists_in_both",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}